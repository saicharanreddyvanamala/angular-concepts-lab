import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  imports: [],
  templateUrl: './home.html',
  styleUrl: './home.scss',
})
export class Home {
/**
 * ANGULAR ROUTING BASICS
 * -------------------------------------------
 * - Angular uses client-side routing (SPA)
 * - Router maps URLs to components
 *
 * Core concepts:
 * - Routes = URL → Component mapping
 * - RouterOutlet = placeholder for routed views
 *
 * Navigation:
 * - routerLink → template navigation
 * - router.navigate() → programmatic navigation
 *
 * Router lifecycle:
 * URL change → route match → component created → outlet render
 */

}
